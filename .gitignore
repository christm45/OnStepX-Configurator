# Claude Code local state
.claude/

# Wrangler local state (API tokens, deploy logs, cached versions)
cloudflare-worker/.wrangler/
**/.wrangler/

# Never commit local secret/token files
*.pem
*token*.txt
*tooken*.txt
*secret*
*.env
.env.*

# Temporary / editor artifacts
*.swp
*.tmp
.DS_Store
Thumbs.db

# CI tooling only — the site itself has no dependencies and no build step
node_modules/
